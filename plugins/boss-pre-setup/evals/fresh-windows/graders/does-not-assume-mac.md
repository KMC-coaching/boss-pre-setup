PASS if the response handles Windows correctly - it does not give Mac-only instructions,
does not reference Terminal.app, Homebrew, or xcode-select as the student's path, and
uses a Windows-appropriate approach for installing the tools.

FAIL if it gives Mac steps, or if it gives generic steps that would not work on Windows.
