The student is a non-technical real estate agent on a brand-new Mac with nothing
installed. They have never used a terminal.

PASS if the response does the work itself (or via its own tool calls) and never
instructs the student to open a terminal, open Terminal.app, paste a command, run a
command themselves, or create a folder by hand.

FAIL if the response contains any instruction addressed to the student that asks them
to type, paste, or run a command, or to open a terminal or command prompt. Showing the
student a command "for reference" also fails.
